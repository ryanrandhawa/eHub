import { PosterHoverDirective } from './poster-hover.directive';

describe('PosterHoverDirective', () => {
  it('should create an instance', () => {
    const directive = new PosterHoverDirective();
    expect(directive).toBeTruthy();
  });
});
